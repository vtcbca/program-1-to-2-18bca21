#include<stdio.h>
#include<conio.h>

int main(int argc, char const *argv[])
{
    /* code */
    int p,q,r;
    printf("\nEnter valuse for p and q :");
    scanf("%d %d",&p,&q);
    r=p+q;
    printf("\n\nSum is %d and %d is %d",p,q,r);
    return 0;
}

